{
  "name": "termlauncher",
  "version": "2.3.0",
  "description": "TermLauncher - 快速開啟專案的桌面工具",
  "main": "src/main/index.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder --win portable",
    "build:installer": "electron-builder --win nsis",
    "build:all": "npm run build && npm run build:installer",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "author": "User",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/xjin9612/TermLauncher.git"
  },
  "homepage": "https://github.com/xjin9612/TermLauncher",
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^24.9.1",
    "eslint": "^9.0.0",
    "globals": "^16.0.0",
    "prettier": "^3.8.1",
    "vitest": "^3.0.0"
  },
  "build": {
    "appId": "com.termlauncher.app",
    "productName": "TermLauncher",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "assets/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        "portable"
      ],
      "icon": "assets/icon.ico"
    },
    "portable": {
      "artifactName": "TermLauncher-Portable.exe"
    }
  }
}
