# TermLauncher UI 設計文檔

> 版本：1.0.0  
> 更新日期：2025-01-24

---

## 1. 設計理念

### 1.1 設計原則

| 原則         | 說明                       |
| ------------ | -------------------------- |
| **簡潔高效** | 減少操作步驟，一鍵完成任務 |
| **資訊清晰** | 目錄資訊一目瞭然           |
| **視覺舒適** | 深色主題，適合長時間使用   |
| **回饋即時** | 每個操作都有明確的視覺回饋 |

### 1.2 設計風格

- **主題**：GitHub Dark 風格
- **圓角**：8-12px，柔和不銳利
- **間距**：寬鬆的留白，提升可讀性
- **動效**：輕量過渡動畫（0.15-0.3s）

---

## 2. 色彩系統

### 2.1 主色板

```css
:root {
  /* 背景色 */
  --bg-primary: #0d1117; /* 主背景 */
  --bg-secondary: #161b22; /* 次要背景（卡片、輸入框）*/
  --bg-tertiary: #21262d; /* 第三層背景（hover 狀態）*/

  /* 邊框色 */
  --border-color: #30363d; /* 通用邊框 */

  /* 文字色 */
  --text-primary: #e6edf3; /* 主要文字 */
  --text-secondary: #8b949e; /* 次要文字、placeholder */

  /* 強調色 */
  --accent-blue: #58a6ff; /* 主要強調色、連結、按鈕 */
  --accent-green: #3fb950; /* 成功狀態 */
  --accent-orange: #d29922; /* 警告、WSL 相關 */
  --accent-red: #f85149; /* 錯誤、刪除 */
}
```

### 2.2 色彩用途

| 色彩            | 用途                                 |
| --------------- | ------------------------------------ |
| `accent-blue`   | 主要按鈕、選中狀態、連結、focus 邊框 |
| `accent-green`  | 成功提示、確認操作                   |
| `accent-orange` | WSL 圖示漸層、警告提示               |
| `accent-red`    | 刪除按鈕、錯誤提示、關閉按鈕 hover   |

---

## 3. 字體系統

### 3.1 字體選擇

```css
/* 主要字體 - 介面文字 */
font-family: 'Noto Sans TC', -apple-system, BlinkMacSystemFont, sans-serif;

/* 等寬字體 - 路徑顯示 */
font-family: 'JetBrains Mono', monospace;
```

### 3.2 字級規範

| 用途       | 大小 | 粗細 | 行高 |
| ---------- | ---- | ---- | ---- |
| 標題列     | 13px | 500  | 1.2  |
| 區塊標題   | 14px | 600  | 1.4  |
| 一般文字   | 14px | 400  | 1.5  |
| 次要文字   | 12px | 400  | 1.4  |
| 標籤 (Tag) | 10px | 500  | 1.2  |

---

## 4. 元件規範

### 4.1 按鈕 (Button)

**主要按鈕**
```css
.btn-primary {
  background: var(--accent-blue);
  color: #fff;
  padding: 12px 20px;
  border-radius: 8px;
  font-weight: 500;
}
```

**次要按鈕**
```css
.btn-secondary {
  background: var(--bg-tertiary);
  border: 1px solid var(--border-color);
  color: var(--text-primary);
}
```

### 4.2 輸入框 (Input)

```css
input, select {
  padding: 12px 14px;
  background: var(--bg-primary);
  border: 1px solid var(--border-color);
  border-radius: 8px;
  color: var(--text-primary);
  font-size: 13px;
}

input:focus {
  border-color: var(--accent-blue);
}
```

### 4.3 目錄卡片 (Directory Card)

```css
.directory-item {
  background: var(--bg-secondary);
  border: 1px solid var(--border-color);
  border-radius: 10px;
  padding: 14px 16px;
}

.directory-item:hover {
  background: var(--bg-tertiary);
  border-color: var(--accent-blue);
  transform: translateX(4px);
}
```

### 4.4 標籤 (Tag)

**WSL 標籤**
```css
.tag.wsl {
  background: rgba(233, 84, 32, 0.2);
  color: #f78166;
}
```

**PowerShell 標籤**
```css
.tag.ps {
  background: rgba(0, 120, 212, 0.2);
  color: #58a6ff;
}
```

### 4.5 Toast 通知

```css
.toast {
  background: var(--bg-tertiary);
  border-radius: 10px;
  padding: 14px 20px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
}

.toast.success { border-color: var(--accent-green); }
.toast.error { border-color: var(--accent-red); }
```

---

## 5. 頁面佈局

### 5.1 整體結構

```
┌─────────────────────────────────────────────┐
│  標題列 (Titlebar) - 36px                    │
├─────────────────────────────────────────────┤
│  標籤頁 (Tabs) - 48px                        │
├─────────────────────────────────────────────┤
│                                             │
│           內容區 (Content)                   │
│           可捲動                             │
│                                             │
└─────────────────────────────────────────────┘
```

### 5.2 標題列 (Titlebar)

```
┌─────────────────────────────────────────────┐
│ 🚀 TermLauncher              [─] [□] [✕]   │
└─────────────────────────────────────────────┘
```

- 左側：Logo + 應用名稱
- 右側：最小化、最大化、關閉按鈕
- 支援拖曳移動視窗

### 5.3 目錄頁面

```
┌─────────────────────────────────────────────┐
│ [🔍 搜尋目錄...]            [所有群組 ▼]    │
├─────────────────────────────────────────────┤
│ ➕ 新增目錄                      [展開]     │
├─────────────────────────────────────────────┤
│ 🕐 最近使用                                 │
│ [🐧 專案A] [⚡ 專案B]                       │
├─────────────────────────────────────────────┤
│ ── 預設 (2) ────────────────────────────── │
│ ┌─────────────────────────────────────────┐ │
│ │ 🐧  我的專案              [WSL]    [🗑️] │ │
│ │     D:\Projects\my-app                  │ │
│ └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

### 5.4 設定頁面

```
┌─────────────────────────────────────────────┐
│ ⚙️ 一般設定                                 │
│ ├ 啟動時最小化                    [  ○]    │
│ ├ 關閉時最小化到托盤              [●  ]    │
│ └ 全域快捷鍵                  [Alt+Space]  │
├─────────────────────────────────────────────┤
│ 🏷️ 群組管理                                 │
│ [預設] [+ 新增群組]                         │
├─────────────────────────────────────────────┤
│ 💾 資料管理                                 │
│ [📤 匯出設定]    [📥 匯入設定]              │
└─────────────────────────────────────────────┘
```

---

## 6. 互動規範

### 6.1 Hover 效果

| 元素     | 效果                           |
| -------- | ------------------------------ |
| 目錄卡片 | 背景變亮 + 藍色邊框 + 右移 4px |
| 按鈕     | 背景變亮                       |
| 刪除按鈕 | 紅色背景 + 紅色文字            |
| 關閉按鈕 | 紅色背景                       |

### 6.2 過渡動畫

| 動作       | 時長  | 曲線     |
| ---------- | ----- | -------- |
| 顏色變化   | 0.15s | ease     |
| 位移動畫   | 0.2s  | ease-out |
| Toast 出現 | 0.3s  | ease-out |

### 6.3 回饋機制

| 操作     | 回饋                          |
| -------- | ----------------------------- |
| 新增目錄 | Toast「目錄已新增」           |
| 刪除目錄 | Toast「目錄已刪除」           |
| 開啟終端 | Toast「正在開啟 XXX...」      |
| 匯出成功 | Toast「設定已匯出」           |
| 錯誤     | 紅色 Toast + 錯誤訊息         |

---

## 7. 圖示規範

### 7.1 圖示對照表

| 用途       | 圖示 |
| ---------- | ---- |
| 應用 Logo  | 🚀   |
| WSL        | 🐧   |
| PowerShell | ⚡   |
| 資料夾     | 📁   |
| 設定       | ⚙️   |
| 新增       | ➕   |
| 刪除       | 🗑️   |
| 時鐘       | 🕐   |
| 標籤       | 🏷️   |
| 儲存       | 💾   |
| 匯出       | 📤   |
| 匯入       | 📥   |

---

## 8. 視窗尺寸

| 類型 | 寬度   | 高度   |
| ---- | ------ | ------ |
| 最小 | 500px  | 600px  |
| 預設 | 700px  | 800px  |
| 最大 | 無限制 | 無限制 |
